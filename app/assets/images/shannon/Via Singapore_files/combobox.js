$(document).ready(function(){
	//$('.combobox').combobox();
	$('.combobox-normal').chosen({
		allow_single_deselect: true
	});

	$('.chosen-min').chosen({
		allow_single_deselect: true,
		width: "54px"
	});

	$('.chosen-small').chosen({
		allow_single_deselect: true,
		width: "80px"
	});

	$('.chosen-medium').chosen({
		allow_single_deselect: true,
		width: "99px"
	});

	$('.chosen-med').chosen({
		allow_single_deselect: true,
		width: "153px"
	});

	$('.chosen-modify').chosen({
		allow_single_deselect: true,
		width: "207px"
	});

	$('.chosen-large').chosen({
		allow_single_deselect: true,
		width: "185px"
	});

	$('.chosen-x-large').chosen({
		allow_single_deselect: true,
		width: "200px"
	});

	
	$('.chosen-massive').chosen({
		allow_single_deselect: true,
		width: "254px"
	});
});
