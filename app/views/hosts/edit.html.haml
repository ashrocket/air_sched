- page_title

.page-header
  = link_to hosts_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to @host, class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-info-sign
    Show
  %h2 Editing Host

= render 'form'
